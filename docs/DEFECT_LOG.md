# DEFECT LOG & REMEDIATION REGISTER
## ERP Lite Hybrid Inventori & Maintenance PJU — DISHUB

**Audit Date**: 2026-09-06  
**Status**: ALL IDENTIFIED DEFECTS REMEDIATED & RETESTED  

---

| ID | Severity | Module | Role | Description | Steps to Reproduce | Root Cause | Fix Applied | Regression Test | Status |
|---|:---:|---|---|---|---|---|---|---|:---:|
| **DEF-001** | **P0** | Database Migrations | All | Kegagalan eksekusi migrasi fresh (`SQLSTATE[42S02] Base table or view not found: 1146 Table 'laravel-gudang.roles' doesn't exist`). | Jalankan `php artisan migrate:fresh` pada basis data bersih. | File migrasi pembuatan tabel dasar (`roles`, `password_reset_tokens`, `failed_jobs`, master `satuans`, `jenis`, `barangs`) tidak lengkap/hilang dari direktori migrations. | Dibuatkan migrasi dasar berurutan standar Laravel (`2014_..._create_roles_table`, `2026_..._create_barangs_table`, dll.) dengan `Schema::hasTable` safety checks. | `CriticalInventoryTest`, `RolePermissionMatrixTest` | **RESOLVED** |
| **DEF-002** | **P0** | Database Migrations | All | Syntax error index SQL pada MySQL 8 (`alamat_jalan(100)`). | Jalankan migrasi `2026_08_09_220000_create_pju_master_tables.php`. | Kolom `alamat_jalan` berjenis VARCHAR(255) di-indeks menggunakan sintaks prefix length yang tidak didukung dalam blueprint Laravel MySQL index array. | Mengganti definisi indeks menjadi `$table->index('kelurahan_id')` yang valid dan bersih. | `php artisan migrate` | **RESOLVED** |
| **DEF-003** | **P0** | Seeding & Factories | All | Database seeder utama tidak ada dan Factory User tidak dapat di-load oleh Pest/PHPUnit. | Jalankan `php artisan db:seed` atau unit test yang memanggil `User::factory()`. | `DatabaseSeeder.php` belum dibuat dan `UserFactory.php` tersimpan di luar direktori `database/factories/`. | Membuat `database/seeders/DatabaseSeeder.php` yang menjalankan `RbacSeeder`, `MasterDataSeeder`, serta memindahkan `UserFactory.php` ke `database/factories/`. | `AuthenticationTest`, `RolePermissionMatrixTest` | **RESOLVED** |
| **DEF-004** | **P1** | User Model / Auth | All | `Attempt to read property "role" on null` dan `Column not found: role_id`. | Registrasi user baru atau pemanggilan `$user->role->role`. | Model `User` hanya mendefinisikan relasi `roles` (many-to-many), sedangkan migration dan controller lama mereferensikan foreign key `role_id` (belongsTo). | Ditambahkan `role_id` ke `$fillable`, relasi fallback `role()`, helper `getPrimaryRole()`, dan event `booted()` auto-sync pivot. | `RegistrationTest`, `RbacTest` | **RESOLVED** |
| **DEF-005** | **P1** | Authorization / RBAC | Teknisi, Viewer | Teknisi dan Viewer tidak memiliki batasan proteksi granular pada Master Data dan Mutasi. | Login sebagai Teknisi, lalu kirim request `POST /barang` atau `DELETE /barang/1`. | Rute Master Data dan Transaksi digabung dalam satu grup `checkRole:superadmin,admin gudang,kepala gudang,teknisi` tanpa pemisahan READ vs WRITE. | Dilakukan restrukturisasi rute web menjadi 12 grup terisolasi; penambahan authorization check pada controller `store/update/destroy`. | `RolePermissionMatrixTest` | **RESOLVED** |
| **DEF-006** | **P1** | Approval Authorization | Teknisi, Admin | Stock Opname dan Retur Vendor dapat di-approve/complete oleh pihak pembuat (Admin) atau Teknisi. | Kirim `POST /stock-opname/{id}/approve` menggunakan akun Admin Gudang atau Teknisi. | Method `StockOpnameController::approve()` dan `ReturVendorController::complete()` tidak melakukan validasi role approver. | Ditambahkan validasi wewenang mutlak manajemen (`superadmin` atau `kepala gudang`) di controller dan rute. | `RolePermissionMatrixTest` | **RESOLVED** |
| **DEF-007** | **P1** | UI / Dead Buttons | Teknisi, Viewer | Halaman Dashboard dan Sidebar Menu kosong bagi role Teknisi dan Viewer; URL relative menyebabkan 404 pada subpath. | Login sebagai Teknisi atau Viewer, atau akses link `barang-masuk` dari halaman edit barang. | Layout sidebar hanya memiliki kondisi `@if` untuk `superadmin`, `kepala gudang`, dan `admin gudang`; tautan menu ditulis tanpa awalan `/`. | Seluruh link diubah menjadi URL absolut (`/barang`, `/laporan-stok`); ditambahkan blok menu terdedikasi untuk `teknisi` dan `viewer`. | `SmokeTest`, `RolePermissionMatrixTest` | **RESOLVED** |
| **DEF-008** | **P2** | Master Data Compatibility | All | SQL Error `Column not found: 1054 Unknown column 'supplier' in 'order clause'` dan `'nama_jenis'`. | Akses halaman master supplier atau jenis barang. | Inkonsistensi nama kolom antara kode lawas (`supplier`, `jenis_barang`) dan kode baru (`nama_supplier`, `nama_jenis`). | Dibuatkan migration synonym columns dan accessor/mutator backward-compatible pada model `Supplier`, `Customer`, dan `Jenis`. | `MasterDataTest` | **RESOLVED** |
| **DEF-009** | **P2** | Dashboard View | All | `Undefined variable $barangMinimum`, `$pimpinanData`, `$warehouseData`, `$technicianData`. | Akses `/dashboard` dengan role Kepala Gudang atau Teknisi. | `DashboardController` tidak mem-passing variabel spesifik per role ke view `dashboard.blade.php`. | Ditambahkan passing dataset lengkap (`pimpinanData`, `warehouseData`, `technicianData`) dengan isolasi metrik aman di controller. | `ReportingAndAuditTest`, `SmokeTest` | **RESOLVED** |
| **DEF-010** | **P2** | Frontend-Backend Parity | Teknisi, Viewer | Tombol "Tambah Barang", "Edit", dan "Hapus" pada UI tetap terlihat meskipun backend menolak. | Buka `/barang` dengan akun Viewer atau Teknisi. | View `barang/index.blade.php` merender tombol aksi secara statis tanpa mengecek permission user yang sedang login. | Ditambahkan kondisional Blade pada tombol Tambah dan Javascript flags `canEdit` & `canDelete` untuk isolasi render tombol baris. | `RolePermissionMatrixTest` | **RESOLVED** |
